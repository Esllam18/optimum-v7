import fs from 'node:fs';import assert from 'node:assert/strict';const p=fs.readFileSync(new URL('../src/v7/pages/ControlPage.js',import.meta.url),'utf8');assert.match(p,/organization_health_snapshot/);assert.match(p,/health\.score/);assert.match(p,/health\.issues/);assert.match(p,/health\.steps/);assert.doesNotMatch(p,/routeNotReadySub/);console.log('V7 organization control 7.0 contract: PASS');
